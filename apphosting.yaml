# Firebase App Hosting — Cloud Run service configuration
runConfig:
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 4
  concurrency: 80

# Environment variables
env:
  - variable: NODE_ENV
    value: production
    availability:
      - RUNTIME
